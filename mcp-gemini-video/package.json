{
  "name": "kg-study-gemini-video",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server that delegates lesson VIDEO understanding (frames + audio) to Google Gemini, so a Claude Code study session can ask questions about a recording it cannot watch.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@google/genai": "^1.5.0",
    "@modelcontextprotocol/sdk": "^1.10.2",
    "zod": "^3.24.3"
  }
}
